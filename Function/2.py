# parameter dan argument
def list(loopDataList): #(parameter)
    for i in loopDataList:
        print(f"ini data list ke- {i}")
dataList = ['satu',2,True,4.4]
list(dataList) #(argument)
# tambah (9,8) contoh dll
